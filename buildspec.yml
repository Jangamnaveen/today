version:0.2

phases:

  install:
    runtime-version:
      java:corretto17

  build:
    commands:
    - echo Build started
    - mvn clean install


  post-build:
    commands:
    - echo Build completed


  artifacts:
    files:
    - target/*.jar
